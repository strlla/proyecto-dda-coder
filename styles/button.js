import { StyleSheet } from "react-native";

export default StyleSheet.create({
  buttonContainer: {
    width: 175,
    marginTop: 20,
    marginBottom: 20,
    marginLeft: "auto",
    marginRight: "auto",
  },
});
